using MEOW_BUSINESS.Services;

namespace MEOW_TESTING;

public class ByteDeserializerTest
{
    [Fact]
    public void DeserializeTextMessageTest()
    {
        /**
        var data = new byte[] { 0x01, 0x00, 0x05, 0x48, 0x65, 0x6c, 0x6c, 0x6f }; 
        var errorService = new MeowErrorService();
        var byteDeserializer = new ByteDeserializer(data, errorService);
        var message = byteDeserializer.Deserialize();
        Assert.Equal(2, message.MessageNumber);    
        */
    }
}